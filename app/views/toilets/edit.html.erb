<h1>トイレ情報の編集</h1>

<% if current_user.id == @user.id %>
  <%= render 'form', toilet: @toilet %>
<% else %>
  <h2>登録者以外は編集できません！！</h2>
<% end %>

<%= link_to 'Show', @toilet %> |
<%= link_to 'Back', toilets_path %>
